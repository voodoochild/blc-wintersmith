extends layout

block prepend title
  | #{ page.title + " | "}

block header
  h1= page.title

block content
  article.post
    p.date
      span= moment(page.date).utc().format(locals.dateFormat)
    section.post-body!= typogr(page.html).typogrify()
  nav
    a(href='/', rel='home') ← Black Lion Chest
