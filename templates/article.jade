extends layout

block prepend title
  | #{ page.title + " – "}

block header
  h1= page.title

block content
  article.article
    p.date
      span= moment().utc(page.date).format('LL')
    section.content!= typogr(page.html).typogrify()
