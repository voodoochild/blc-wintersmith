extends layout

block prepend title
  | #{ page.title + " – "}

block header
  a.brand(href=locals.url, rel='home')= locals.name
  h1= page.title

block content
  article.article
    p.date
      span= moment().utc(page.date).format('LL')
    section.content!= typogr(page.html).typogrify()
